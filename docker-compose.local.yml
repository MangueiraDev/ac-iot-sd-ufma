# AC-IoT SD UFMA — stack local de desenvolvimento
# Plataforma InterSCity: cidadesinteligentes.lsdi.ufma.br (remota, não local)
# Produção/larga escala: use os manifests Kubernetes em k8s/.

services:

  # ── Broker MQTT (TCP 1883 + WebSocket 9001) ────────────────────────────────
  mosquitto:
    image: eclipse-mosquitto:latest
    ports:
      - "1883:1883"   # MQTT TCP (simulator, bridge, Node-RED)
      - "9001:9001"   # MQTT WebSocket (dashboard browser)
    volumes:
      - ./docker/mosquitto/config:/mosquitto/config:ro
      - mosquitto_data:/mosquitto/data
      - mosquitto_log:/mosquitto/log
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "mosquitto_pub -h localhost -t _health -m ok -q 0"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ── Simulador de sensores IoT (rooms carregados do YAML) ──────────────────
  simulator:
    build:
      context: ./src/simulator
    volumes:
      - ./config:/config:ro
    environment:
      - MQTT_BROKER=mosquitto
      - MQTT_PORT=1883
      - PUBLISH_INTERVAL=30       # segundos entre publicações
      - ROOM_COUNT=1000           # escala alvo: 1 000 salas simultâneas
      - SHARD_INDEX=0
      - SHARD_COUNT=1
      - CONFIG_PATH=/config/rooms.yaml
    depends_on:
      mosquitto:
        condition: service_healthy
    restart: unless-stopped

  # ── Bridge MQTT → InterSCity UFMA (pipeline assíncrono, sem bloquear MQTT) ─
  bridge:
    build:
      context: ./src/bridge
    volumes:
      - ./config:/config:ro
    environment:
      - MQTT_BROKER=mosquitto
      - MQTT_PORT=1883
      - MQTT_TOPIC=$$share/bridge-workers/ac-iot/+/sensores
      - INTERSCITY_BASE_URL=https://cidadesinteligentes.lsdi.ufma.br/interscity_lh
      - INTERSCITY_SSL_VERIFY=false
      - INTERSCITY_REQUIRED=false
      - INTERSCITY_REGISTER_RESOURCES=true
      - ROOM_COUNT=1000
      - HTTP_WORKERS=8
      - MAX_INTERSCITY_RPS=20
      - QUEUE_MAX=20000
      - CONFIG_PATH=/config/rooms.yaml
    depends_on:
      mosquitto:
        condition: service_healthy
    restart: unless-stopped

  # ── Dashboard web em tempo real (Nginx serve static) ──────────────────────
  web:
    build:
      context: ./src/web
    ports:
      - "8080:80"     # Acesso: http://localhost:8080
    volumes:
      - ./src/web/static:/usr/share/nginx/html:ro
      - ./src/web/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/ > /dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Node-RED (opcional — automações avançadas) ────────────────────────────
  nodered:
    image: nodered/node-red:latest
    ports:
      - "1880:1880"
    environment:
      - TZ=America/Sao_Paulo
    volumes:
      - ./docker/nodered/data:/data
    depends_on:
      mosquitto:
        condition: service_healthy
    restart: unless-stopped
    profiles:
      - nodered   # docker compose -f docker-compose.local.yml --profile nodered up -d

volumes:
  mosquitto_data:
  mosquitto_log:
